{
  "PostToolUse": [
    {
      "matcher": "Write|Edit",
      "hooks": [
        {
          "type": "prompt",
          "prompt": "If the file just written/edited is a .toml file that appears to be a Nuon app configuration (contains fields like 'type', 'name', 'terraform_version', 'helm_repo', 'runner_type', '[[input]]', '[[group]]', 'runner_type', 'chart_name', 'namespace', or template expressions like '{{ .nuon'), briefly check for: (1) invalid Go template syntax - every {{ must have matching }}, (2) common typos like '.nuon.input.inputs' (should be '.nuon.inputs.inputs') or 'terrafrom_version' or 'helm_rpo', (3) missing required 'name' or 'type' field for components. Only mention real issues found. Be very concise (1-2 lines). If no issues or not a Nuon config file, say nothing."
        }
      ]
    }
  ]
}
